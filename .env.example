# GitHub App Configuration
# For local testing, you can use placeholder values
# To test with real GitHub, create a GitHub App at: https://github.com/settings/apps/new
#
# GITHUB_APP_ID: For local testing, use any number (e.g., 123456). 
#                For production, use your actual GitHub App ID (numeric, shown in app settings)
GITHUB_APP_ID=123456

# GITHUB_PRIVATE_KEY: For local testing, use any placeholder PEM-formatted string.
#                     For production, use your actual RSA private key from GitHub App settings.
#                     IMPORTANT: Replace actual newlines with \n in the .env file
#                     Format: "-----BEGIN RSA PRIVATE KEY-----\n...key content...\n-----END RSA PRIVATE KEY-----"
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIEpAIBAAKCAQEA...\n-----END RSA PRIVATE KEY-----"

# GITHUB_WEBHOOK_SECRET: For local testing, use any random string.
#                        For production, use the webhook secret from your GitHub App settings.
#                        This must match the secret configured in your GitHub App webhook settings.
GITHUB_WEBHOOK_SECRET=test_webhook_secret_12345

# GITHUB_INSTALLATION_ID: Optional. Leave empty for local testing.
#                        For production, get this after installing the app on a repository.
#                        Found in the installation URL or via API.
GITHUB_INSTALLATION_ID=

# Service API Configuration
# These can be any random strings for local testing
# For production, use secure randomly generated secrets

# SERVICE_HMAC_SECRET: Secret for signing requests between GitHub App and Service API.
#                     For local testing, any string works (e.g., test_service_hmac_secret_12345).
#                     For production, generate a secure random string: openssl rand -hex 32
SERVICE_HMAC_SECRET=test_service_hmac_secret_12345

# BOT_API_TOKEN: Bearer token for authenticating manual job triggers via API.
#                For local testing, any string works (e.g., test_bot_api_token_12345).
#                For production, generate a secure random string: openssl rand -hex 32
BOT_API_TOKEN=test_bot_api_token_12345

PORT=3000
NODE_ENV=development

# LlamaStack Configuration
# For local testing with mock mode, use these values (no real API needed)
# For production, get API key from your LlamaStack account

# LLAMA_URL: For local testing, use https://mock.llamastack.com/v1 (enables mock mode).
#            For production, use your actual LlamaStack API URL.
LLAMA_URL=https://mock.llamastack.com/v1

# LLAMA_API_KEY: For local testing with mock mode, any value works (e.g., mock_api_key_for_testing).
#                For production, use your actual LlamaStack API key from your account dashboard.
LLAMA_API_KEY=mock_api_key_for_testing

LLAMA_MODEL=llama-3.1-70b
LLAMA_MAX_TOKENS=4096

# Queue & Database
# These match the docker-compose.yml defaults
# For local testing, use these values as-is
# For production, update with your actual Redis and PostgreSQL connection strings

REDIS_URL=redis://localhost:6379
DATABASE_URL=postgresql://llamaforge:llamaforge_dev@localhost:5432/llamaforge

# Feature Flags
# DRY_RUN: Set to true to prevent actual PR creation (safe for testing).
#          Set to false for production to allow real PR creation.
DRY_RUN=true

# Observability (optional)
LOG_LEVEL=debug
SENTRY_DSN=
